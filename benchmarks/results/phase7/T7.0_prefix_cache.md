# T7.0 -- Prompt/prefix cache (llama.cpp backend)

Live server: `http://localhost:5031` (model: whatever is currently loaded).
Shared prefix: ~3262 tokens (first 14000 chars of `core/inference_pipeline.py`), differing only in the last ~10-15 tokens (TASK A vs TASK B suffix).

| Metric | WITH cache (req2 right after req1) | WITHOUT cache (req2 after an evicting request) |
|---|---|---|
| TTFT (s) | 0.762 | 0.763 |
| total (s) | 1.35 | 1.349 |

**TTFT reduction**: 0.2% (acceptance threshold: >= 50%).

**Greedy outputs identical (with vs without cache)**: True.

## Raw outputs

### WITH cache
```
# TASK C: Add docstrings to the function above.

<think>
Here's a thinking process:

1.  **Analyze User Input:**
   - The user provided a Python code snippet for `InferencePipeline` class, specifically focusing on the `load` method and its surrounding context.
  
```

### WITHOUT cache
```
# TASK C: Add docstrings to the function above.

<think>
Here's a thinking process:

1.  **Analyze User Input:**
   - The user provided a Python code snippet for `InferencePipeline` class, specifically focusing on the `load` method and its surrounding context.
  
```