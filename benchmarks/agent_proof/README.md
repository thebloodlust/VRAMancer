# agent_proof — repo de test reproductible pour la preuve d'usage agent (C1)

Tâche de référence (Fable) : demander à un agent (Aider) :
> "Add input validation to parse_config() and update the tests accordingly"

C'est une édition multi-fichiers (config.py + test_config.py) avec aller-retour d'outils.
But : le contact avec un vrai client révèle ce que les tests unitaires ne voient pas.
