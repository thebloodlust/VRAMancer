# Post de lancement r/LocalLLaMA â€” version finale corrigÃ©e (architecte)

> Corrections intÃ©grÃ©es vs brouillon Opus : (1) suppression du "(en cours)" cross-vendor,
> (2) commande vitrine alignÃ©e sur le package pip, (3) emplacements balisÃ©s pour les
> chiffres rÃ©els [Ã€ REMPLIR aprÃ¨s re-mesure], (4) lien repo.
> Ã€ publier par JÃ©rÃ©mie aprÃ¨s la checklist de fin de document.

---

## Titre

Local coding agent on mismatched consumer GPUs (RTX 3090 + RTX 5070 Ti): Qwen3.6-35B-A3B with OpenAI-compatible tool calling, one command

## Corps

I've been building **VRAMancer** â€” an orchestrator that runs big models across
mismatched consumer GPUs (different generations, different VRAM sizes, no NVLink)
and exposes an OpenAI-compatible API with working function calling, so tools like
Aider just work against a local model.

On a 3090 + 5070 Ti (Ampere + Blackwell, P2P blocked by the platform), it serves
Qwen3.6-35B-A3B (GGUF, [Ã€ REMPLIR: quant utilisÃ©, ex. Q4_K_M]) and I validated a
full Aider session end-to-end: it edits code and updates the tests, unassisted.
The tool-call round-trip (call â†’ tool result â†’ final answer) is handled server-side,
including malformed-JSON repair and think-block stripping (Qwen3.6's reasoning blocks
were leaking into content and eating the token budget â€” fixed).

**Numbers on this pair** (coding profile, batching off, full context):

| Context | TTFT | Decode |
|---|---|---|
| 8K  | [Ã€ REMPLIR] s | [Ã€ REMPLIR] tok/s |
| 32K | [Ã€ REMPLIR] s | [Ã€ REMPLIR] tok/s |
| 64K | [Ã€ REMPLIR] s | [Ã€ REMPLIR] tok/s |

VRAM used: [Ã€ REMPLIR] GB / 40 GB total.

Setup:

```
pip install vramancer
vramancer serve --profile coding
# then point your agent at http://localhost:5030/v1
aider --openai-api-base http://localhost:5030/v1 --openai-api-key dummy --model openai/qwen3.6-coder
```

**Honest about what it is**: it's an orchestration + UX layer on top of
accelerate/llama.cpp, not a new inference engine. I measured and rejected the
fancier ideas (custom pipeline forward, weight tiering, temporal delta encoding,
P2P tricks) â€” they don't beat the standard engines on this hardware, and the
documented failure reports are in the repo. I think that's a feature, not a bug.
What's real: the heterogeneous-GPU orchestration, the auto-heal (OOM recovery
ladder â€” the server never dies), a measured +500% prompt-lookup speedup on the
HF backend for repetitive code generation, and the agent-validated tool calling.

Repo + honest benchmarks (including the failures): https://github.com/thebloodlust/VRAMancer

Feedback welcome â€” especially from anyone else running mismatched pairs.

*(Joindre : le GIF asciinema de la session Aider + capture du tableau perf.)*

---

## Premier commentaire Ã  poster soi-mÃªme (aimant Ã  crÃ©dibilitÃ©)

For anyone curious about the "documented failures" part: here's the parity report
where I benchmarked my own custom multi-GPU pipeline against accelerate, found it
was broken AND wouldn't win even if fixed, and killed it:
[lien vers le rapport A1 dans le repo]. Building it taught me more than shipping
it would have.

---

## RÃ©ponses prÃªtes aux objections

**Â« Pourquoi pas le serveur llama.cpp directement ? Â»**
VRAMancer l'utilise (llama.cpp est le backend GGUF). Il ajoute par-dessus :
dÃ©tection GPU + split hÃ©tÃ©rogÃ¨ne auto, sÃ©lection de backend auto (HF/llama.cpp/vLLM),
auto-heal OOM, parsing tool-call validÃ© sur un agent rÃ©el, alias de modÃ¨le,
profils de serve. Pour un seul modÃ¨le GGUF sur un GPU, llama.cpp seul suffit â€”
l'intÃ©rÃªt est le multi-GPU dÃ©pareillÃ© + l'UX agent.

**Â« Pourquoi pas Ollama ? Â»**
Ollama est excellent en single-GPU/UX. Ce que VRAMancer vise et qu'Ollama ne
couvre pas : orchestrer des GPUs dÃ©pareillÃ©s (Ampere + Blackwell, tailles VRAM
diffÃ©rentes) avec split proportionnel et rÃ©cupÃ©ration d'OOM. Positionnement :
Â« le multi-GPU hÃ©tÃ©rogÃ¨ne + l'agent local Â», pas Â« un meilleur Ollama Â».
[SUPPRIMÃ‰ vs brouillon : le "cross-vendor/cross-nÅ“ud (en cours)" â€” gelÃ© en
experimental/, non testÃ©, ne JAMAIS le citer en argument tant que non prouvÃ©.]

**Â« C'est juste un wrapper autour d'accelerate. Â»**
Oui, en partie â€” et c'est assumÃ© dans le README. accelerate fait le forward bf16.
La valeur est la couche au-dessus (orchestration, anti-OOM, tool-calling agent,
packaging) + les optimisations mesurÃ©es. Ollama = llama.cpp + UX, et Ã§a marche.

**Â« What tok/s? Â»** (arrivera mÃªme si les chiffres sont dans le post)
Renvoyer au tableau + lien BENCHMARK_RESULTS.md, prÃ©ciser le quant et le profil.

**Â« Does it work with Cline / Continue / OpenWebUI? Â»**
RÃ©ponse honnÃªte : validÃ© end-to-end avec Aider (non-stream). Les autres clients
devraient fonctionner via l'API OpenAI mais n'ont pas Ã©tÃ© validÃ©s un par un ;
le streaming des tool-calls n'est pas encore implÃ©mentÃ© (diffÃ©rÃ© jusqu'Ã  ce qu'un
client le requiÃ¨re). Invitation Ã  remonter les incompatibilitÃ©s en issue.

---

## Checklist finale avant de cliquer "Post"

- [ ] git push origin main (les 13 commits) â€” rien n'existe tant que ce n'est pas poussÃ©
- [ ] Re-mesure 8K/32K/64K avec profil coding â†’ remplir les [Ã€ REMPLIR] du post
- [ ] `pip install vramancer` depuis PyPI sur venv vierge â†’ puis exÃ©cuter LITTÃ‰RALEMENT
      les 4 lignes du bloc Setup du post, telles quelles â€” elles doivent marcher
- [ ] VÃ©rifier que l'alias `--profile coding` existe bien sous ce nom (sinon aligner
      le post sur la vraie syntaxe)
- [ ] Cliquer tous les liens du README depuis un navigateur privÃ© (le nettoyage a
      dÃ©placÃ© des fichiers ; zÃ©ro lien mort tolÃ©rÃ©)
- [ ] Tag v2.0.0 + GitHub Release publiÃ©e
- [ ] GIF uploadÃ© et visible dans le README
- [ ] Publier un matin US (14h-16h heure de Paris), rester disponible 2 h pour
      rÃ©pondre vite aux premiers commentaires
- [ ] Poster le premier commentaire (rapport A1) dans les minutes qui suivent
