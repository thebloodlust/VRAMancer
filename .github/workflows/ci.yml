name: VRAMancer CI

on:
  push:
    branches: [ main, master, v7* ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.9", "3.10", "3.11"]
      fail-fast: false

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt || true
        pip install pytest pytest-cov mock aiohttp aiohttp[speedups] pytest-asyncio
        
    - name: Run tests (fast)
      env:
        VRM_MINIMAL_TEST: "1"
        VRM_DISABLE_RATE_LIMIT: "1"
        VRM_TEST_MODE: "1"
        VRM_BACKEND_ALLOW_STUB: "1"
      run: |
        pytest tests/ -m "not (slow or integration or chaos)" --no-cov -q

    - name: Run tests (full unfiltered suite, Ubuntu only)
      if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.10'
      env:
        VRM_MINIMAL_TEST: "1"
        VRM_DISABLE_RATE_LIMIT: "1"
        VRM_TEST_MODE: "1"
        VRM_BACKEND_ALLOW_STUB: "1"
      run: |
        pytest tests/ --no-cov -q

  lint:
    name: Python lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    - name: Install flake8
      run: pip install flake8
    - name: Run flake8
      run: flake8 core/ vramancer/ --max-line-length=120 --ignore=E501,W503,E402,F401,F811 --count --statistics || true
