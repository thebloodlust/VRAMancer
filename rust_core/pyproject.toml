[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "vramancer_rust"
version = "0.1.0"
description = "VRAMancer Rust native extensions (Zero-Trust P2P, Software CXL, HMAC)"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
