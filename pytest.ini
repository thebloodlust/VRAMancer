[pytest]
addopts = -q --cov=core --cov-report=term-missing --cov-fail-under=50
markers =
    slow: tests lents ou gourmands
    integration: tests d'intégration
    network: tests réseau
    smoke: sous-ensemble rapide (sanity de base)