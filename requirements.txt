#############################################################
# VRAMancer requirements (lite / base server profile)
# - Version minimale pour exécution orchestrateur + API + mémoire
# - GUI / Dash / Vision / Tracing / Compression avancée déplacés dans:
#     requirements-full.txt  OU  pip install .[all]
#
# Pour stack complète: `pip install -r requirements-full.txt`
#############################################################

# Core
torch==2.2.0
numpy==1.26.0
pyyaml==6.0.1
psutil==5.9.8
transformers==4.34.0
accelerate==0.27.2
flask==3.0.2
pydantic==2.8.2
pycryptodome==3.20.0
prometheus-client==0.21.0
rich==13.9.4
requests==2.32.3
python-socketio==5.11.1
PyJWT==2.9.0

# Tests (optionnel local) – en CI installés via extra [test]
pytest==8.2.0
pytest-cov==5.0.0
flake8==7.1.0

# Packaging
setuptools==69.0.3
