# Requirements spécifiques pour Windows - Dashboards VRAMancer
# Toutes les dépendances nécessaires pour faire fonctionner les dashboards
# Installation: pip install -r requirements-windows.txt

# Core dependencies
torch==2.2.0          # Wheel available for win_amd64 cp312
numpy==1.26.0
pyyaml==6.0.1
psutil==5.9.8
# Explicit newer tokenizers before transformers to lock compatible wheel
tokenizers==0.20.1    # Has Windows wheels for Py3.12
transformers==4.46.2  # Compatible with tokenizers 0.20.x
accelerate==0.27.2
flask==3.0.2
pydantic==2.8.2
pycryptodome==3.20.0
prometheus-client==0.21.0
rich==13.9.4
requests==2.32.3
python-socketio==5.11.1
PyJWT==2.9.0

# Dashboard GUI dependencies - CRITIQUE pour dashboards !
PyQt5==5.15.10
flask-socketio==5.10.0
eventlet==0.35.2
python-engineio==4.9.0

# WebSocket et networking
websockets==12.0
aiohttp==3.9.1

# Windows-specific utilities
pywin32; sys_platform == "win32"
colorama; sys_platform == "win32"

# Image support pour Qt dashboards
Pillow==10.0.0

# Development tools
pytest==8.2.0
pytest-cov==5.0.0
flake8==7.1.0
setuptools==69.0.3
click==8.1.7

# Notes:
# - If you still need transformers 4.34.0 pin, install Rust + Visual Studio Build Tools (C++), then use original requirements.txt.
# - To force slow (pure Python) tokenizers behavior, export USE_SLOW_TOKENIZER=1 before launching (fallback already handled in utils).
